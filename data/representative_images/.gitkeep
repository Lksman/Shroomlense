we should save some representative images for each class in the dataset here, the images will be displayed in the UI
4-5 images per class should suffice as we just want some examples to cycle through in the UI.
